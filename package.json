{
  "name": "appwrite-migration",
  "version": "1.0.0",
  "type": "module",
  "description": "Appwrite database setup and migration script",
  "scripts": {
    "migrate": "node migrate-to-appwrite.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "optionalDependencies": {
    "@libsql/client": "^0.5.0"
  }
}
