{
  "version": 2,
  "buildCommand": "mkdir -p public && cp *.html *.js *.css *.min.js public/ && mkdir -p public/presets && cp -r presets/* public/presets/ && mkdir -p public/utils && cp -r utils/* public/utils/ && mkdir -p public/api/content && cp api/content/*.js public/api/content/",
  "devCommand": "vercel dev",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/presets/(.*)",
      "dest": "/presets/$1"
    },
    {
      "src": "/utils/(.*)",
      "dest": "/utils/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
